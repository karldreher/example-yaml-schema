[tool.poetry]
name = "my-cool-app"
version = "0.1.0"
description = ""
authors = ["Karl Dreher]
readme = "README.md"

[tool.poetry.dependencies]
python = "^3.12"

[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
